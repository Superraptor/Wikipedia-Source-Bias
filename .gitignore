*/.pytest_cache/
.pytest_cache/
*/.vscode/
__pycache__/
*copy/
output*
results*
# `pip install .` (requirements.txt installs the local package) leaves these.
build/
*.egg-info/
.env
node_modules/
.nuxt/
.output/
dist/
# `nuxt generate` drops a dist -> .output/public symlink; a trailing-slash
# pattern does not match a symlink, so it needs its own entry.
frontend/dist
# Generated by `npm run build` from frontend/.output/public. The Toolforge
# buildpack regenerates it during the image build, so it must not be committed.
backend/static/
.idea/
*.egg-info/
country_coordinates_cache.json
wikidata_publisher_cache.json
wikidata_enrichment_cache.json
wikidata_author_cache.json
nametrace_cache.json
crossref_cache.json
page_cache.json
__pycache__/
*.pyc
